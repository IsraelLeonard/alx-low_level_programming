#include <stdio.h>
/**
 * main - prints the size of various types
 * Description: A program prints the size of various types on the computer it is compiled
 * Return: 0
 */
int main(void)
{
char a;
init b;
long init c;
long long init d;
printf("size of char: %zu byte(s)\n", size of (a));
printf("size of init: %zu byte(s)\n", size of (b));
printf("size of long init: %zu byte(s)\n", size of (c));
printf("size of long long init: %zu byte(s)\n", size of (d));
printf("size of float: %zu byte(s)\n", size of (float));
return (0);
}
