#include "main.h"

/**
 * add - A program that adds two integers and returns the result
 * @n: first digit for the summation
 * @m: second digit for the summation
 * Return: The result of yhe summation
 */
int add(int n, int m)
{
	int x;

	x = n + m;
	return (x);
}
