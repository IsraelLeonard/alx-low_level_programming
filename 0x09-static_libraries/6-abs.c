#include "main.h"

/**
 * _abs - To compute absolute value
 * @n: The integer whose value is sought
 * Return: The absolite value
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
		return (-n);
}
